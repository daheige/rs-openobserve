apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-openobserve-0
  namespace: openobserve
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: hostpath
  resources:
    requests:
      storage: 10Gi
