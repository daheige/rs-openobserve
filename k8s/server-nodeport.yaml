# 新增 NodePort（用于外部访问）
apiVersion: v1
kind: Service
metadata:
  name: openobserve-nodeport
  namespace: openobserve
spec:
  type: NodePort
  selector:
    app: openobserve
  ports:
    - name: http
      port: 5080
      targetPort: 5080
      nodePort: 30080
